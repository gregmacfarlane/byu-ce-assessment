{
  "name": "byu-ce-assessment",
  "private": true,
  "scripts": {
    "render": "quarto render",
    "protect": "node scripts/protect-site.mjs",
    "build:protected": "npm run render && npm run protect",
    "publish:protected": "npm run build:protected && quarto publish gh-pages --profile protected --no-render --no-browser"
  },
  "devDependencies": {
    "staticrypt": "^3.5.4"
  }
}
